min_radius: 20  # 圆环最小半径
max_radius: 100  # 圆环最大半径
dp: 1.5  # 累加器分辨率与图像分辨率的反比（值越小，检测越精确但速度越慢）
min_dist_ratio: 8.0  # 最小圆心距离与图像高度的比例
param1: 100  # 边缘检测阈值（值越小，检测到的边缘越多）
param2: 30  # 累加器阈值（值越小，检测到的圆环越多）
